/*
 * $Id: Imakefile,v 1.2 1993-03-24 22:38:55 granger Exp $
 *
 * Basically just install config.h into the include directory,
 * and make sure the necessary files are distributed.
 * Note how the scripts subdirectory is included in the distribution
 * without actually performing a 'make distfiles' in ./scripts
 */

#define IHaveSubdirs
#define PassCDebugFlags

SUBDIRS = scripts

all::

DISTFILES = Imakefile Project.tmpl README README.imake Xosdefs.h \
	    Zeb-Imake.doc Zeb.rules config.h scripts

BuildIncludes(config.h Xosdefs.h,.,.)

depend::

install::

lint::

Makefiles::

DistfilesTarget($(DISTFILES))

