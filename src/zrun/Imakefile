
SRCS = zrun.c
OBJS = zrun.o

LIBS = $(LIBZEB) $(LIBRDSS)

DISTFILES = Imakefile $(SRCS)

NormalProgramTarget(zrun, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))

DependTarget()
LintTarget()
DistfilesTarget($(DISTFILES))
