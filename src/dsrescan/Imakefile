/*
 * Imakefile for the data store dumper
 */

SRCS = dsrescan.c
OBJS = dsrescan.o

LIBS = $(LIBZEBDS) $(LIBZEB) $(LIBRDSS) $(LIBNETCDF)
DISTFILES = $(SRCS) Imakefile

NormalProgramTarget(dsrescan, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))
DistfilesTarget($(DISTFILES))
DependTarget()
LintTarget()
