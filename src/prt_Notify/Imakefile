/*
 * Imakefile for the data store dumper
 */

SRCS = prt_Notify.c
OBJS = prt_Notify.o

LIBS = $(LIBZEBDS) $(LIBZEB) $(LIBRDSS) $(LIBNETCDF)

INCLUDES = -I$(TOP)/src/ds

all::

install::

NormalProgramTarget(prt_Notify, $(OBJS), $(DEPLIBS), $(LIBS), /**/)
DependTarget ()

DISTFILES = $(SRCS) Imakefile
DistfilesTarget($(DISTFILES))
