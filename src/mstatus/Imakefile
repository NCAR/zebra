
SRCS = mstatus.c
OBJS = mstatus.o

LIBS = $(LIBZEBRA)
DISTFILES = $(SRCS) Imakefile

CompleteProgramTarget(mstatus, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))
LintTarget()
DependTarget()
DistfilesTarget($(DISTFILES))
