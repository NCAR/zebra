
SRCS = mstatus.c
OBJS = mstatus.o

LIBS = $(LIBZEB) $(LIBRDSS)
DISTFILES = $(SRCS) Imakefile

NormalProgramTarget(mstatus, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))
LintTarget()
DependTarget()
DistfilesTarget($(DISTFILES))
