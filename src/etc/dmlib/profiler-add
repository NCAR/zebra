!
! Add data to a profiler window.
!
procedure profiler-add platform string field string window string
!
! OK, throw it in to all components in the window.
!
        local comps pd_complist(window)
        foreach comp #comps
           parameter #window #comp platform #platform
        endfor
endprocedure
