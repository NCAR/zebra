/*
 * PD library files.
 */

PDFILES = p_contour.pd p_fcontour.pd p_irg_winds.pd p_kmgrid.pd p_llgrid.pd \
	p_map.pd p_radar.pd p_raster.pd p_rings.pd p_simple_track.pd \
	p_station.pd p_track.pd


DISTFILES = $(PDFILES) Imakefile

all::	/* nothing */

InstallLibFile ($(PDFILES),pd,..)

DistfilesTarget ($(DISTFILES))
