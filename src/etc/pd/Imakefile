/*
 * PD library files.
 */
all::	/* nothing */

install::
	MakeDir ($(LIBDIR)/pd)
	for pd_file in *.pd ; do \
		$(INSTALL) $${pd_file} $(LIBDIR)/pd ; \
	done

DISTFILES = Imakefile *.pd

DistfilesTarget($(DISTFILES))
