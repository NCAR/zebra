
SRCS = zstop.c
OBJS = zstop.o

LIBS = $(LIBZEBRA)
DISTFILES = $(SRCS) Imakefile

CompleteProgramTarget(zstop, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))
DistfilesTarget($(DISTFILES))
DependTarget()
LintTarget()
