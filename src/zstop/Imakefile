
SRCS = zstop.c
OBJS = zstop.o

LIBS = $(LIBZEB) $(LIBRDSS)
DISTFILES = $(SRCS) Imakefile

NormalProgramTarget(zstop, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))
DistfilesTarget($(DISTFILES))
DependTarget()
LintTarget()
