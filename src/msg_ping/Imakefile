
SRCS = msg_ping.c
OBJS = msg_ping.o

LIBS = $(ZLIBS)
DISTFILES = $(SRCS) Imakefile

CompleteProgramTarget(msg_ping, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))
DependTarget()
DistfilesTarget($(DISTFILES))
