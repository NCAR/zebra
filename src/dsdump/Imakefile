/*
 * Imakefile for the data store dumper
 */

SRCS = dsdump.c
OBJS = dsdump.o

LIBS = $(LIBZEBDS) $(LIBZEB) $(LIBRDSS) $(LIBNETCDF) $(LIBREGEXP)
DISTFILES = $(SRCS) Imakefile

NormalProgramTarget(dsdump, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))
DistfilesTarget($(DISTFILES))
DependTarget ()
LintTarget()
