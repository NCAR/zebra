/*
 * Imakefile for the message daemon
 */

SRCS = message.c
OBJS = message.o

LIBS = $(LIBZEB) $(LIBRDSS)

DISTFILES = Imakefile $(SRCS)

NormalProgramTarget(message, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))

DependTarget()
LintTarget()
DistfilesTarget($(DISTFILES))
