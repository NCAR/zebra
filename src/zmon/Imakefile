
SRCS = zmon.c
OBJS = zmon.o

LIBS = $(LIBZEB) $(LIBRDSS) $(XLIBS)

DISTFILES = Imakefile $(SRCS)

CompleteProgramTarget(zmon, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))

DependTarget()
LintTarget()
DistfilesTarget($(DISTFILES))
