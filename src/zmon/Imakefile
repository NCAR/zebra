
SRCS = zmon.c
OBJS = zmon.o

LIBS = $(LIBZEB) $(LIBRDSS) $(XLIBS)

DISTFILES = Imakefile $(SRCS)

NormalProgramTarget(zmon, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))

DependTarget()
LintTarget()
DistfilesTarget($(DISTFILES))
