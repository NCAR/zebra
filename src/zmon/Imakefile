
SRCS = zmon.c
OBJS = zmon.o

INCLUDES = $(XINCLUDES)
LIBS = $(LIBZEB) $(XLIBS)

DISTFILES = Imakefile $(SRCS)

CompleteProgramTarget(zmon, $(OBJS), $(DEPLIBS), $(LIBS), /**/)

DependTarget()
LintTarget()
DistfilesTarget($(DISTFILES))
