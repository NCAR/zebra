
SRCS = zquery.c
OBJS = zquery.o

LIBS = $(LIBZEB) $(LIBRDSS)

DISTFILES = Imakefile $(SRCS)

NormalProgramTarget(zquery, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))

DependTarget()
LintTarget()
DistfilesTarget($(DISTFILES))
