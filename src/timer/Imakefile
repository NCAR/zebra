
SRCS = timer.c
OBJS = timer.o

LIBS = $(ZLIBS)
DISTFILES = $(SRCS) Imakefile

CompleteProgramTarget(timer, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))
DistfilesTarget($(DISTFILES))
DependTarget()
LintTarget()
