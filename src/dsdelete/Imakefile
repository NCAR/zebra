/*
 * Imakefile for the data store dumper
 */

SRCS = dsdelete.c
OBJS = dsdelete.o

LIBS = $(LIBZEBDS) $(LIBZEB) $(LIBRDSS) $(LIBNETCDF) $(XLIBS)

DISTFILES = $(SRCS) Imakefile

NormalProgramTarget(dsdelete, $(OBJS), $(DEPLIBS), $(LIBS), $(SYSLIBS))
DistfilesTarget($(DISTFILES))
DependTarget()
LintTarget()
